// Enable prerendering - page will show demo preview when not authenticated
export const prerender = true;

// Server-side load function for add expense page
export async function load() {
	return {};
}
